using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DisplayManager : MonoBehaviour {

	// Use this for initialization
	void Start () {
		/*GameObject Malcolm = Instantiate(Resources.Load("Malcolm")) as GameObject;
		Malcolm.name = "Malcolm";
		Debug.Log(Malcolm.name);
		GameObject Remy = Instantiate(Resources.Load("Remy")) as GameObject;
		Remy.name = "Remy";
		Debug.Log(Remy.name);
		Remy.transform.localPosition = new Vector3(3,0,0);
		Malcolm.transform.localPosition = new Vector3(-1,0,0);*/
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
